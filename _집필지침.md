# PPT → 교재 md 집필 지침 — LLM 심화

강의 슬라이드(개조식 단문)를 **읽는 교재**로 다시 쓴다. 요약이 아니라 **재서술**이다.
ADA 교재(26개 절)에서 쓴 지침을 이 덱에 맞게 고친 것이다. **다른 점을 §0 에 모아 두었으니
먼저 읽을 것.**

## 0. ADA 교재와 다른 점 — 여기부터 읽는다

1. **장 번호가 바뀌었다.** 원본은 Chapter 4·5 지만 교재는 **1·2장**이다.
   슬라이드 본문에 `4.2`, `Chapter 5` 같은 구 번호가 나오면 **새 번호로 고쳐 쓴다.**
   (구 번호를 그대로 두면 독립 교재로서 앞뒤가 맞지 않는다.)
2. **이 덱의 실습에는 번호가 없다.** "실습 : LangGraph 응용" 처럼 제목만 있다.
   그래서 실습 번호는 **새로 부여**하고(`실습 2.3.1`), 노트북 파일명을 반드시 병기한다.
   노트북 파일명은 **교재 번호와 일치**시킨다(예: `code/2.3.1.LangGraph_basic.ipynb`).
   원본 강의 자료 `code/` 는 구 번호(4.x·5.x) 그대로 두고, 배포 트리 `교재_md/code/` 에
   새 번호 사본을 둔다 (사용자 결정, 2026-09).
3. **실습 설명은 보강해도 된다** (§9). 이 덱은 실습 슬라이드가 "제목 한 줄 + 코드 스크린샷"
   으로만 되어 있는 곳이 많다. 그 자리는 설명을 채워 넣는다. 다만 **사실·수치·인용은 여전히
   지어내지 않는다** — 경계는 §2 에 있다.
4. **`fig…`(슬라이드 통째 렌더링)가 68장이나 나왔지만 대부분 쓰지 않는다** (§6).
   이 덱은 코드 스크린샷이 여러 장 겹친 슬라이드가 많아 `is_diagram()` 이 과하게 잡았다.

## 1. 파일 골격

```markdown
# 2.3 LangGraph 활용

**학습 목표**

- (슬라이드에서 읽어낸 도달점 3~5개, "…할 수 있다" 형태)

**이 절의 구성**

- 2.3.1 …
- 2.3.2 …

---

## 2.3.1 첫 소절 제목
<!-- 슬라이드 111~113 -->

본문 …

---

## 2.3.N 정리
```

- 제목 위계: 파일 최상단 `#` 은 절 제목 하나뿐. 소절은 `##`, 그 아래는 `###`.
- **슬라이드 1장 = 소절 1개가 아니다.** 내용이 이어지는 2~5장을 묶어 하나의 소절로 만든다.
- 소절 사이는 `---` 로 나눈다.

## 2. 문체와 지어내기 금지

- 개조식 단문("…함")을 평서형 서술문("…한다")으로 고친다.
- 슬라이드의 들여쓰기 위계는 문단 구조로 옮긴다. 불릿을 그대로 베끼지 않는다.
  다만 병렬 항목의 나열(도구 목록, 조건 목록, 단계)은 불릿을 유지하는 편이 낫다.
- 영어 용어는 처음 나올 때 `한글(English)` 로 쓰고 이후에는 한쪽으로 통일한다.

**넣어도 되는 것과 안 되는 것의 경계**

| 넣어도 된다 | 넣지 않는다 |
|---|---|
| 코드가 무엇을 하는지에 대한 설명 | 원문에 없는 **수치·성능·벤치마크 결과** |
| 실습의 목적·전제·읽는 법 | 원문에 없는 **논문명·저자·연도·URL** |
| 개념 사이의 연결 문장 | 원문에 없는 **주장의 출처 귀속** |
| 라이브러리 API 의 일반적 동작 설명 | 원문이 다루지 않은 **새 개념·새 절** |

- **이 규칙은 그림 alt 텍스트와 캡션에도 똑같이 적용된다.** 캡션은 본문보다 검토를 덜
  받으니 더 조심할 것. ADA 에서 두 번 사고가 났다 — 없는 출처를 캡션에 적었고,
  그림을 열어 보지 않고 쓴 alt 가 내용과 달랐다. **캡션은 그림을 실제로 열어 보고 쓴다.**
- 원문이 틀렸다고 판단되면 말없이 고치지 말고 `<!-- 원문 불일치 확인 필요: … -->` 를 남긴다.
  명백한 오탈자·비문은 예외로 고친다.

## 3. 강조 블록

원문이 "핵심 주장", "요약", "주의", "정의" 성격일 때는 인용 블록으로 뽑는다.

```markdown
> **핵심 메시지**
> LangGraph 의 State 는 노드가 주고받는 값이 아니라, 그래프 전체가 공유하는 하나의 사전이다.
```

## 4. 표

원문 덤프의 `[표]` 아래 마크다운 표는 **그대로 살린다.** 셀 안 줄바꿈이 공백으로 합쳐져
있으니 읽기 좋게 다듬는다. 표 바로 앞에 그 표가 무엇을 비교하는지 한 문장을 둔다.

## 5. 수식

이 덱은 수식이 20개뿐이고 대부분 1.3(LoRA)에 있다. OMML 자동 변환이라 거칠다.

- 문장 속 기호는 인라인 `$r$`, 독립된 식은 `$$ … $$`.
- `\sqrt (a)(b)` → `\sqrt{(a)(b)}`, `Σ_j` → `\sum_j` 처럼 변환 흔적을 고친다.
- 수식이 깨져 뜻을 알 수 없으면 버리지 말고 문장으로 풀어 쓴 뒤 `<!-- 원문 수식 확인 필요 -->`.

## 6. 그림 — 이 덱의 가장 큰 함정

`images/` 에 실제 파일이 들어 있다. 원문 덤프의 표기를 그대로 쓴다.

| 원문 덤프 표기 | 뜻 | 교재에 넣는 법 |
|---|---|---|
| `[그림: sNNN_MM.png · 12.3x7.1cm]` | 슬라이드에 삽입된 그림 | `![설명](images/sNNN_MM.png)` |
| `[개념도 렌더링: figNNN.png]` | 슬라이드를 통째로 캡처한 것 | **대부분 쓰지 않는다** ↓ |
| `[장식: … ]` | 아이콘·로고 (2cm 미만) | 넣지 않는다 |

- **`fig…` 를 그대로 넣지 마라.** 제목 띠·불릿·쪽번호까지 교재에 실린다.
  이 덱에서 68장이 나왔지만 그중 대부분은 **코드 스크린샷이 여러 장 겹친 슬라이드**일 뿐
  개념도가 아니다. 진짜 도형 관계도일 때만 `render.crop_slide()` 로 개념도 영역만 잘라
  `sNNN_MM.png` 형식으로 저장해 쓴다.
- **코드 스크린샷은 넣지 않는다.** 노트북의 실제 코드로 대체한다 (§9).
- **결과 이미지(그래프·실행 결과 캡처)는 그대로 넣는다.** 코드로 대체할 수 없다.
- **글자만 있는 캡처는 본문으로 옮긴다.** 검색도 복사도 안 되는 그림으로 둘 이유가 없다.
- 이미지는 그 내용을 설명하는 문장 **바로 뒤**에 둔다. 슬라이드 순서가 아니라 서술 흐름을 따른다.
- alt 텍스트는 파일명이 아니라 **그림이 무엇을 보여주는지** 쓴다.
- 원문 덤프에 적힌 파일명은 `images/` 에 반드시 실재한다. 파일명을 지어내지 않는다.

**그림 크기.** md 에 폭을 안 적으면 뷰어가 원본 픽셀대로 그린다(CSS 96dpi → 글자 h픽셀
= 0.75h pt). 즉 캡처 해상도가 곧 교재의 글자 크기다. 집필이 끝난 뒤 `imgsize` 로 재서
폭을 정리한다. 집필 중에는 신경 쓰지 않아도 된다.

## 7. 슬라이드 출처 주석 — 반드시 넣는다

소절 제목 **바로 아래 줄**에 그 소절이 어느 슬라이드에서 나왔는지 적는다.

```markdown
## 2.3.4 오류 재시도 루프
<!-- 슬라이드 122 -->
```

- HTML 주석이라 렌더링에는 보이지 않는다. 학생용 지면은 그대로다.
- 이것이 있어야 나중에 사람이 원문과 대조할 수 있다. 없으면 통독하는 수밖에 없고,
  ADA 에서 실제로 그 때문에 누락을 못 잡았다.
- 범위가 떨어져 있으면 `<!-- 슬라이드 111~113, 118 -->` 처럼 쉼표로 나열한다.
- **절의 모든 슬라이드가 어느 한 소절에는 배정되어야 한다.** `audit.trace_report()` 가 잡는다.

## 8. 실습은 본문 흐름 안에 둔다

**실습을 절 뒤로 몰지 않는다.** 원문 슬라이드 순서를 따라, 그 실습이 증거로 쓰이는 설명
바로 뒤에 놓는다. 실습은 곁가지 연습이 아니라 논지의 일부다. 떼어 놓으면 "왜 이 실습을
하는가"가 사라지고, 독자는 그것을 **누락으로 인식한다.**

- 실습 번호는 **새로 부여**한다: 절 번호 + 일련번호 (`실습 2.3.1`, `실습 2.3.2` …).
- 실습 제목 바로 아래에 **노트북 파일명을 인용 블록으로 병기한다.**
  ```markdown
  ### 실습 2.3.1 — LangGraph 기본 구조
  <!-- 슬라이드 113~117 -->

  > 노트북: `code/2.3.1.LangGraph_basic.ipynb`
  ```
- 본문의 노트북 파일명은 배포 트리 `교재_md/code/` 의 **새 번호 사본**을 가리킨다.
  원본 `code/`(구 번호 4.x·5.x)는 바꾸지 않는다. 대응표는 `config.NOTEBOOK_MAP` 와
  `_추가검토.md` 의 rename 표.
- 실습과제(슬라이드 18·30·56 의 "실습과제 : …")는 성격이 다르므로 **절 맨 뒤에 모은다.**

## 9. 실습 코드 — 스크린샷 대신 실제 코드, 설명은 보강한다

`_원문/N.M_실습코드.txt` 에 `code/` 노트북의 실제 코드가 들어 있다.

```markdown
### 실습 1.2.1 — Trainer 로 Fine-tuning 하기

> 노트북: `code/1.2.1.FineTuning.ipynb`

학습에 필요한 설정은 `TrainingArguments` 하나에 모은다. 배치 크기와 epoch 수처럼
학습 속도에 직접 영향을 주는 값이 여기 들어간다.

​```python
training_args = TrainingArguments(...)
​```
```

- 노트북 셀을 통째로 다 옮기지 말고 **슬라이드가 보여 주던 부분**을 고른다.
  상용구(폰트 설정·`!pip install`)는 이미 걸러져 있다.
- **설명이 부족한 곳은 채워 넣는다.** 이 덱의 실습 슬라이드는 "3) Training Arguments 설정"
  처럼 제목 한 줄에 스크린샷 하나인 경우가 많다. 그대로 옮기면 교재가 아니라 목록이 된다.
  코드가 무엇을 하는지, 왜 그 순서인지, 어떤 값을 바꾸면 무엇이 달라지는지를 쓴다.
  **1.2 절이 특히 그렇다** (13장에 본문 1,481자, 그림 30장).
- 다만 §2 의 경계는 지킨다. 채워 넣는 것은 **설명**이지 사실이 아니다.
  성능 수치, 논문 인용, 벤치마크 결과를 새로 만들어 넣지 않는다.
- 노트북에만 있고 슬라이드에는 없는 **주제**는 넣지 않는다. 교재의 범위 기준은 슬라이드다.
  (같은 주제 안에서 코드를 더 보여 주는 것은 괜찮다.)

## 10. 원문의 구조 표지를 지우지 않는다

원문에 `Part A : …` 같은 부(部) 구분이나 `review` 매듭이 있으면 md 에서도 보이게 한다.

```markdown
---

**⟨ Part B ⟩ …**

> 도입 한두 문장
```

제목(`##`)이 아니라 굵은 글씨 + 구분선으로 두는 이유는 소절 번호 체계를 흐트러뜨리지
않기 위해서다. `#` 은 절 제목 하나, `##` 은 소절이라는 규칙은 유지한다.

## 11. 하지 말 것

- 슬라이드 번호를 **본문 글자로** 노출하지 않는다. 출처는 §7 의 HTML 주석으로만.
  (파일 머리말의 원본 슬라이드 범위 인용 블록도 쓰지 않는다 — 사용자 결정, 2026-09.)
- "이 슬라이드에서는", "다음 장에서" 같은 발표 화법을 쓰지 않는다.
- 원문에 없는 절/소절을 새로 만들지 않는다.
- 내용을 요약해서 줄이지 않는다. **분량은 원문 본문자수 이상**이 되는 것이 정상이다.
- 구 장 번호(Chapter 4·5, 4.2, 5.3 …)를 본문에 그대로 남기지 않는다 (§0-1).
